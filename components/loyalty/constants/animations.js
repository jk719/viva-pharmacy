/**
 * @deprecated This file is no longer used for animations. All animation-related code
 * was removed in April 2025 as part of the loyalty banner cleanup. CSS animations are
 * now used instead of Framer Motion for simpler maintenance.
 * 
 * If animations need to be re-implemented, create a new file with a clear, descriptive
 * name rather than modifying this deprecated file.
 */

// Empty placeholder objects to prevent import errors in any legacy code
export const ANIMATIONS = {};
export const BADGE_ANIMATIONS = {}; 